while getopts t: option
do
    case "${option}"
    in
        t) TIME=${OPTARG};;
    esac
done

if [ -n "$TIME" ]
then
    export gtdtime=$TIME
else
    export gtdtime=25
fi

tmux new-session "tmux source-file ~/.tmux-session-config"
